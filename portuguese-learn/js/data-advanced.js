/* Falar — ADVANCED content (famous-song vocabulary/culture + daily dialogues).
   Placeholder until the verification workflow finishes; will be overwritten with
   authored + double-audited content. Merges into the PT.data set from data.js. */
window.PT = window.PT || {};
PT.data = PT.data || {};
PT.data.songs = PT.data.songs || [];
PT.data.dialogues = PT.data.dialogues || [];
